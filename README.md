# RDG Remont

Static website for RDG Remont.

## GitHub Pages

Publish from the repository root. The main page is `index.html`.

If uploading through a text-only GitHub integration, use `index-embedded.html` as `index.html`; it contains the images as embedded data URLs.
